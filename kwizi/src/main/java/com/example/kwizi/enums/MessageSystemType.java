package com.example.kwizi.enums;

public enum MessageSystemType {
    REGULAR,
    USER_ADDED,
    USER_REMOVED,
    USER_LEFT,
    GROUP_TITLE_CHANGED,
    USER_PROMOTED,
    USER_DEMOTED,
    GROUP_PHOTO_CHANGED
}